extends layout

block content
    .advert
        .desc
            .advert-img
                img(src="/images/picture.png")
            h3= advert.title
            h1 #{advert.price + 'zł'}
            
            h3 Opis
            p #{advert.desc}
        
        .contact
            .contact-user
                h4 Użytkownik
                h3
                    a(href="3") #{advert.owner.name}
                a(href="#", class="btn btn-primary") Napisz wiadomość
                a(href="#", class="btn") 
                    span Zadzwoń
